I've pinned the versions for the core scientific stack and included the `alpaca-py` library which your script relies on for data fetching.

```text
# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0

# Data Fetching
alpaca-py>=0.12.0

# Utilities
python-dateutil>=2.8.2